<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
</head>
<body>
    <h1>Sign Up</h1>
    <form action = "/profiles" method = 'POST'>
        <label for= "name">Name:</label>
        <input type = "text" name = "name" id = "name" placeholder = "Profile name">
        <label for = "townLocation">Town Location:</label>
        <input type = "text" name = "townLocation" id = "townlocation" placeholder = "Town Location">
        
        <label for = "location">Where Can You Volunteer?</label>
        
        <select name = "location" id="location">
            <% for(let category of categories1) { %>
                <option value = "<%= category %>"><%= category %></option>
            <% } %>
            
        </select>  
    
        <label for = "zipCode">Zip Code:</label>
        <input type = "number" name = "zipCode" id = "zipCode" placeholder = "Zip Code">
       <h2></h2>
        <label for = "interestTag1, interestTag2, interestTag3">Choose Three Interests:</label>
        <select name = "interestTag1" id="interestTag1">
            <% for(let category of categories2) { %>
                <option value = "<%= category %>"><%= category %></option>
            <% } %>
        </select>
        <select name = "interestTag2" id="interestTag2">
            <% for(let category of categories2) { %>
                <option value = "<%= category %>"><%= category %></option>
            <% } %>
        </select>
        <select name = "interestTag3" id="interestTag3">
            <% for(let category of categories2) { %>
                <option value = "<%= category %>"><%= category %></option>
            <% } %>
        </select>                  
        <input type="submit">
    </form>
</body>
</html>